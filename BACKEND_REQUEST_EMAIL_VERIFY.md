# [요청] 이메일 인증 + 비밀번호 재설정 — 프론트 → 백엔드

작성 2026-08-13 · 갱신 2026-08-14 (7번 비밀번호 재설정 추가) · 프론트 담당

---

## 0. 세 줄 요약

1. 지금 아무 이메일로나 가입이 되는데, **카드 접근 권한이 이메일로 결정되기 때문에 이게 권한 우회 통로가 됩니다.**
2. 막는 데 꼭 필요한 건 **`emailVerified` 플래그 하나와 카드 조회 조건 한 줄**입니다. 인증 메일은 그다음입니다.
3. 시간이 없으면 **1단계(플래그 + 조회 조건)만** 해주셔도 구멍은 닫힙니다. 프론트는 어느 쪽이든 대응합니다.

> **7번에 비밀번호 재설정(비밀번호 찾기) 요청을 덧붙였습니다.** 3단계 인증 메일과
> 토큰 발급·만료·1회용 처리·메일 발송이 완전히 같은 구조라, 따로 부탁드리면 같은 것을
> 두 번 만드시게 됩니다. 그래서 한 문서에 묶었습니다.

---

## 1. 왜 필요한가 — 지금 실제로 뚫려 있는 것

`GET /api/handover-cards/{id}` 명세에 이렇게 적혀 있습니다.

> 카드를 만든 소유자 또는 **receiverEmail로 연결된 수신자**만 조회할 수 있습니다.
> 권한이 없으면 카드 존재 여부를 숨기기 위해 403이 아닌 404를 반환합니다.

즉 **"그 이메일을 가진 계정"이 곧 열람 권한**입니다. 그런데 `POST /api/auth/signup` 은 이메일 소유를 확인하지 않으므로,

```
1. 공격자가 victim@company.com 으로 가입한다 (인증 절차 없음)
2. 그 계정으로 로그인한다
3. victim 앞으로 온 인계 카드가 카드 목록에 그대로 뜬다
```

가 성립합니다. 인계 카드 본문은 업무 내용·블로커·계정 정보가 들어갈 수 있는 자리라 열람 권한이 이렇게 새면 곤란합니다.
404로 존재를 숨기는 처리까지 해두신 걸 보면 열람 권한을 신경 쓰신 것 같아, 같은 맥락에서 드리는 요청입니다.

---

## 2. 1단계 — 구멍만 막기 (작업량 작음, 이것만이라도)

### 2-1. 회원에 인증 여부 추가

| 항목 | 내용 |
|---|---|
| 컬럼 | `member.email_verified BOOLEAN NOT NULL DEFAULT false` |
| 응답 | `MemberResponse` 에 `emailVerified: boolean` 추가 |

`MemberResponse` 는 `GET /api/members/me` 와 `POST /api/auth/signup` 이 함께 쓰는 스키마라 한 번만 고치면 됩니다.

### 2-2. 카드 조회 권한에서 미인증 이메일을 빼기

`receiverEmail` 로 접근 권한을 판정하는 곳(카드 단건 조회 + 카드 목록)에 조건을 하나 더 겁니다.

```
기존:  card.receiverEmail = me.email
변경:  card.receiverEmail = me.email  AND  me.emailVerified = true
```

**소유자 본인의 접근은 건드리지 않습니다.** 내가 만든 카드는 인증 여부와 무관하게 계속 보여야 합니다.

### 2-3. 소셜 로그인 계정은 처음부터 인증된 것으로

`POST /api/auth/oauth2/{provider}` 로 만들어지는 계정은 **구글·깃헙이 이미 이메일을 검증한 계정**이므로
생성 시 `emailVerified = true` 로 넣어 주세요. (기존 회원에 연결하는 경우에도 `true` 로 올려주시면 좋습니다.)

여기까지만 해도 "남의 이메일로 가입해 카드 훔쳐보기"는 막힙니다.

---

## 3. 2단계 — 인증 메일 (여유가 되면)

### 3-1. 인증 메일 보내기

```
POST /api/auth/email/verification
Authorization: Bearer {accessToken}
```

- 로그인한 본인의 이메일로 인증 메일을 보냅니다. 가입 직후 프론트가 한 번 호출하고, 사용자가 "다시 보내기"를 누르면 또 호출합니다.
- 이미 인증된 계정이면 **409**.
- 재발송 남용 방지로 **60초 안에 다시 호출하면 429**. (프론트에서도 버튼을 60초 잠그지만 서버에서도 막아 주세요.)

응답 200:

```json
{ "sentTo": "user@example.com", "expiresInSeconds": 86400 }
```

### 3-2. 토큰 검증

```
POST /api/auth/email/verification/confirm
Content-Type: application/json

{ "token": "메일 링크에 담긴 토큰" }
```

- **인증 없이 호출 가능**해야 합니다. 메일을 다른 기기·다른 브라우저에서 열 수 있어서, 로그인 상태를 요구하면 자주 막힙니다.
- 성공 200. 응답 본문은 없어도 되고, 주신다면 `MemberResponse` 가 편합니다(프론트가 곧바로 화면 상태를 갱신할 수 있음).
- 실패는 **400** + 아래 3-4의 본문. **만료와 이미 사용됨을 문구로 구분해 주세요** — 사용자에게 다른 안내를 띄워야 합니다.

토큰 요건: 추측 불가능한 랜덤(32바이트 이상), **유효기간 24시간, 1회용**, 재발송하면 이전 토큰 무효화.

### 3-3. 메일 링크는 프론트 주소로

메일 본문의 링크를 이 형식으로 만들어 주세요.

```
{프론트주소}/verify-email?token={토큰}
```

프론트가 그 화면에서 토큰을 꺼내 3-2를 호출하고 결과를 보여줍니다. **이미 `/oauth2/callback/{provider}` 를 같은 구조로 처리하고 있어서 붙이는 비용이 거의 없습니다.**

프론트 주소는 배포 후 확정해서 알려드리겠습니다. **하드코딩하지 마시고 환경변수로 빼 주세요** — 로컬 개발은 `http://localhost:5173`, 배포는 별도 도메인이라 둘 다 필요합니다.

> 백엔드에서 검증하고 프론트로 리다이렉트하는 방식(`{백엔드}/api/auth/email/verify?token=...` → 302)도 괜찮습니다. 그게 편하시면 그쪽으로 하고 리다이렉트 주소만 알려 주세요.

### 3-4. 에러 응답 본문 — 겸사겸사 부탁

지금 명세는 **모든 엔드포인트가 200만 정의**하고 에러 본문 형식이 없습니다. 그래서 프론트는 `message` → `error` → `detail` → `title` 순으로 훑어보고 다 없으면 상태코드별 기본 문구로 때우고 있습니다.

이번에 추가하는 엔드포인트만이라도 이 형식으로 주시면 사용자에게 정확한 문구를 띄울 수 있습니다.

```json
{ "message": "인증 링크가 만료되었습니다. 다시 보내기를 눌러 주세요." }
```

기존 엔드포인트까지 같은 형식으로 통일해 주시면 더 좋습니다.

---

## 4. 정하고 넘어가야 할 것 — 미인증 계정으로 로그인은 되게 해주세요

**미인증 상태에서도 로그인과 카드 생성은 되어야 합니다.** 가입 직후 인증 메일을 확인하기 전에도 앱을 둘러볼 수 있어야 하고, 무엇보다 **8/17 데모 촬영 때 메일함을 오가는 장면이 끼면 흐름이 끊깁니다.**

제한은 **`receiverEmail` 로 남의 카드를 열람하는 것 하나만**으로 충분합니다. 그게 실제 구멍이고, 나머지를 막으면 얻는 것 없이 불편만 늘어납니다.

프론트에서는 미인증 계정에 안내 띠와 "인증 메일 다시 보내기" 버튼을 띄울 계획입니다.

---

## 5. 프론트가 할 일 (백엔드 확정 후)

| 순서 | 내용 |
|---|---|
| 1 | `MemberResponse` 타입에 `emailVerified` 추가 |
| 2 | 미인증 계정에 안내 띠 + 재발송 버튼 (60초 잠금) |
| 3 | `/verify-email` 화면 — 토큰 검증, 성공·만료·이미 사용됨 각각 안내 |
| 4 | 설정 화면에 인증 상태 표시 |

**엔드포인트 이름이나 응답 모양은 편하신 대로 바꾸셔도 됩니다.** 확정된 형태만 알려주시면 프론트는 거기에 맞춥니다. 위 이름들은 기존 `/api/auth/*` 결을 따라 지어본 제안입니다.

---

## 6. 우선순위

마감이 **8/17**이라 이렇게 나눠 보시면 좋겠습니다.

| | 내용 | 없으면 |
|---|---|---|
| **필수** | 2단계 (플래그 + 카드 조회 조건 + 소셜 계정 true) | 권한 우회가 그대로 남습니다 |
| 권장 | 3단계 (인증 메일) | 구멍은 막혔지만 아무도 인증을 못 받습니다 |
| 권장 | **7단계 (비밀번호 재설정)** | 비밀번호를 잊으면 계정을 되찾을 방법이 아예 없습니다 |
| 선택 | 3-4 (에러 본문 형식) | 프론트가 지금처럼 추측해서 문구를 띄웁니다 |

**필수만 해주셔도 발표에서 "권한 검증까지 처리했다"고 말할 수 있습니다.**

**3단계와 7단계는 메일 발송·토큰 관리를 그대로 공유합니다.** 한쪽을 만들어 두면 다른 쪽은
엔드포인트 두 개와 메일 본문만 더 얹으면 되므로, **같이 하시는 편이 훨씬 쌉니다.**
둘 중 하나만 하실 수 있다면 **3단계를 먼저** 부탁드립니다 — 그쪽이 실제로 뚫려 있는 구멍을 막습니다.

시간이 정말 안 되시면 **소셜 로그인 리다이렉트 URI 등록**(별건, `PROJECT_STATUS.md` 9번)만이라도 부탁드립니다. 구글·깃헙 계정은 공급자가 이메일을 검증해 주므로, 그것만으로도 검증된 계정을 받는 길은 열립니다.

---

## 7. 비밀번호 재설정 (비밀번호 찾기)

### 7-1. 지금은 프론트에서 만들 방법이 없습니다

현재 명세에서 비밀번호를 다루는 엔드포인트는 하나뿐입니다.

```
PUT /api/members/me/password
Authorization: Bearer {accessToken}

{ "currentPassword": "...", "newPassword": "..." }
```

**로그인한 사람이 바꾸는** 기능입니다. 비밀번호를 잊은 사람은 `currentPassword` 도 액세스 토큰도
없으므로 이 엔드포인트를 쓸 수 없습니다. 재설정 토큰을 만들고 메일로 보내고 검증하는 일이
전부 서버에서 일어나야 해서, **프론트 단독으로는 어떤 방법으로도 대체할 수 없습니다.**

### 7-2. 재설정 메일 보내기

```
POST /api/auth/password/reset
Content-Type: application/json

{ "email": "user@example.com" }
```

- **인증 없이 호출 가능**해야 합니다 (로그인을 못 하는 사람이 쓰는 기능입니다).
- **가입되지 않은 이메일이어도 200 을 주세요.** 404 를 주면 "이 이메일이 이 서비스에 가입돼
  있는지" 확인하는 통로가 됩니다. 같은 이유로 응답에 `sentTo` 같은 값도 넣지 말아 주세요.
  프론트도 "가입된 계정이면 메일을 보냈습니다" 처럼 존재 여부를 드러내지 않는 문구를 씁니다.
- 재발송 남용 방지로 **60초 안에 다시 호출하면 429**. (프론트에서도 버튼을 60초 잠급니다.)

응답 200:

```json
{ "expiresInSeconds": 1800 }
```

### 7-3. 토큰으로 새 비밀번호 설정

```
POST /api/auth/password/reset/confirm
Content-Type: application/json

{ "token": "메일 링크에 담긴 토큰", "newPassword": "새 비밀번호" }
```

- **인증 없이 호출 가능**해야 합니다. 3-2 와 같은 이유입니다 — 메일을 다른 기기·다른 브라우저에서 엽니다.
- 비밀번호 규칙은 가입과 동일하게 **8~100자** 로 맞춰 주세요 (`SignupRequest.password` 기준).
  프론트에서 먼저 걸러도 서버에서 한 번 더 막아 주시면 좋습니다.
- 성공 **200**. 응답 본문은 없어도 됩니다.
- 실패 **400** + 3-4 의 본문. **만료 / 이미 사용됨 / 비밀번호 규칙 위반**을 문구로 구분해 주세요.
  사용자에게 띄울 안내가 각각 다릅니다.
- **성공하면 그 회원의 리프레시 토큰을 전부 무효화해 주세요.** 비밀번호를 재설정하는 상황은
  계정을 빼앗겼을 때일 수 있는데, 기존 세션이 살아 있으면 비밀번호를 바꿔도 침입자는 그대로 남습니다.

토큰 요건: 추측 불가능한 랜덤(32바이트 이상), **유효기간 30분, 1회용**, 재발송하면 이전 토큰 무효화.

> 인증 메일(24시간)보다 짧게 잡았습니다. 재설정 토큰은 그 자체가 계정 접근 권한이라,
> 메일함이 잠깐 열려 있던 사이에 쓰이면 곤란합니다.

### 7-4. 메일 링크는 프론트 주소로

```
{프론트주소}/reset-password?token={토큰}
```

3-3 과 같은 구조입니다. 프론트 주소는 **하드코딩하지 마시고 환경변수로** 빼 주세요.

### 7-5. 정하고 알려 주세요 — 소셜 로그인으로만 가입한 계정

구글·깃헙으로만 가입한 계정은 비밀번호가 없을 수 있습니다. 그 이메일로 재설정 요청이 오면
어떻게 할지 정해야 합니다.

- **권장**: 응답은 그대로 **200** 을 주고(7-2 의 이유 — 여기서 다르게 응답하면 가입 방식이 새어 나갑니다),
  **메일 본문만** "이 계정은 구글 로그인을 사용합니다" 로 보내기.
- 어느 쪽으로 정하시든 알려 주시면 프론트 문구를 맞추겠습니다.

### 7-6. 프론트가 할 일 (백엔드 확정 후)

| 순서 | 내용 |
|---|---|
| 1 | 로그인 화면에 「비밀번호를 잊으셨나요?」 링크 |
| 2 | 이메일 입력 화면 → 발송 안내 (계정 존재 여부를 드러내지 않는 문구, 재발송 60초 잠금) |
| 3 | `/reset-password` 화면 — 새 비밀번호 입력, 성공·만료·이미 사용됨 각각 안내 |
| 4 | 성공 후 로그인 화면으로 이동 |

화면 전체가 한국어·영어 2개 국어라 **문구는 양쪽 모두 준비**합니다.

**엔드포인트 이름이나 응답 모양은 편하신 대로 바꾸셔도 됩니다.** 5번과 마찬가지로,
확정된 형태만 알려주시면 프론트는 거기에 맞춥니다.
