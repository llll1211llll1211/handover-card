# Handover Card 프론트엔드 - 진행 상황 정리 (2026-08-14 갱신)

> 새 대화를 시작할 때 이 파일을 그대로 업로드하거나 내용을 붙여넣으면
> Claude가 지금까지 상황을 바로 이해하고 이어서 도와줄 수 있습니다.

## 1. 프로젝트 개요

- **제품명**: Handover Card
- **한 줄 정의**: 외국인 개발자와 협업하는 개발자들을 위한 **비동기 음성 인계 서비스**
- **핵심 플로우**: 인계자가 음성으로 인계 내용 녹음 → STT(텍스트 변환) → 번역 →
  LLM으로 요약(핵심 포인트/액션 아이템/블로커) → 서버 저장 → 인수자가 "인계 카드" 형태로 확인
- **소속/행사**: Campus AX-Ton (한국기술교육대학교) 해커톤
- **마감**: **2026-08-17까지 제품 소개 영상(PPT 데모용) 촬영 필요**

## 2. 팀 구성

- **나(사용자)**: 프론트엔드 담당. 컴공 전공자.
- **백엔드 담당**: PRD·Swagger 명세·API 서버 구현 및 배포 완료.

## 3. 현재 상태 — MVP 동작 확인 완료 ✅

2026-08-12 기준 **전체 플로우가 실제로 통했습니다.**
로그인 → 녹음 → 업로드 → STT → 번역 → 요약 → 카드 상세 표시까지 실제 서버로 검증했고,
카드 status 가 `COMPLETED` 까지 도달하는 것을 확인했습니다.

### 기술 스택 (결정 완료)

- **Vite 6 + React 19 + TypeScript** (상태관리 라이브러리 없음 — Context 로 충분)
- react-router-dom 7
- CSS 직접 작성 (UI 라이브러리 없음). 모바일 반응형.
- 코드는 Claude 가 작성하고 사용자가 리뷰하는 방식으로 진행

### 다국어 — 한국어 / 영어 (2026-08-14 추가)

**화면 전체가 두 언어로 나온다.** 인계를 받는 쪽이 외국인 동료라는 제품 전제와 맞물리는
기능이라, 데모에서 "왜 이 서비스가 필요한가" 를 화면만으로 보여줄 수 있다.

- 문구는 `src/i18n/messages.ts` 한 곳에 모여 있다. 영어 묶음을 한국어 묶음의 타입으로
  못박아 뒀으므로 **한쪽에만 문구를 추가하면 빌드가 깨진다.** 자리표시자가 필요한 문구는
  함수라 인자 개수까지 타입이 지킨다.
- 훅을 못 쓰는 자리(`api/client.ts`, `useAudioRecorder`, `lib/format.ts`)를 위해
  모듈 통로 `messages()` 를 따로 뒀다.
- 고른 언어가 없으면 브라우저 설정을 따른다. **한국어 사용자가 아니면 영어로 열린다.**
  고른 값은 localStorage 에 남고 `<html lang>` 도 함께 바뀐다.
- **요약 문장도 고른 언어로 보인다.** 서버가 항목마다 원문·번역문 두 벌을 다 주므로
  화면 언어와 같은 쪽을 골라 보여주면 된다 (`lib/summary.ts`). 새로 번역하거나 API 를
  더 부르지 않는다.
- 언어 이름(`한국어` / `English`)과 데크 표시(`READY`/`REC`/`DONE`)는 번역하지 않는다.
  앞은 각 언어의 자기 이름이고 뒤는 기계 표시다.

### 디자인 방향 — 「세이지 & 앰버」 (결정 완료)

짙은 슬레이트가 주 색, 앰버는 시선을 끌어야 하는 자리에만 곁들인다.
민트빛이 도는 흰 바탕이 화면을 눌러 줘서 인계 내용이 먼저 읽힌다.

| 역할 | 값 |
|---|---|
| 바탕 (민트 화이트) | `#E8ECEB` |
| 표면 | `#FFFFFF` |
| 주 색 (슬레이트) | `#2D3E4E` |
| 곁들임 (앰버) | `#E09E50` — 글자·아이콘용 진한 단계 `#C07C2C` |
| 완료 (세이지) | `#3F7F79` |
| 진행 | `#9A6520` |
| 실패 | `#B04B3A` — 팔레트 밖. 네 색에 붉은 계열이 없는데 실패는 구분돼야 한다 |

따뜻한 색은 `--accent-warm` 이라는 **역할 이름**으로 부른다. 팔레트를 또 바꾸더라도
값만 갈아 끼우면 되도록(전에 `--coral` 로 뒀다가 앰버로 바뀌며 이름이 어긋난 적이 있다).

**밝은 톤 하나로 고정했다.** OS 다크 모드에 따라 바뀌면 데모 촬영 때 화면이 예측되지 않는다.

**카드 상세에서 언어 토글을 없앴다.** 읽는 사람의 언어를 크게, 나머지 한 벌을 그 아래 작게
함께 보여준다. "번역이 됐다" 가 한눈에 보여야 하는 화면에서 한쪽을 감추는 것은 손해다.

**음성 입력 데크** — 녹음과 파일 올리기가 같은 골격(`.deck`)을 쓴다.
위에 상태 표시 + 카운터, 그 아래 선, 선 밑에 가로로 긴 작업 영역, 맨 아래 안내문.
가운데 정렬을 버리고 왼쪽부터 채워 세로 공간을 아낀다.

- 녹음: `READY` → `REC`(앰버, 점 깜빡임) → `PAUSED` → `DONE`
- 파형은 **왼쪽부터 차오르다가** 끝까지 차면 왼쪽으로 밀려 나간다
- 큰 버튼이 멈춤·이어하기, **왼쪽 작은 네모가 녹음 완료**. 잠깐 멈추려다 끝내는 사고를 막는다
- 실제로 열린 마이크 이름이 초시계 옆에 붙는다
- **이모지를 전부 뺐다.** OS 마다 모양·색이 달라 아이콘은 SVG 로 직접 그린다

**재생기와 드롭다운도 직접 만들었다** (`AudioPlayer.tsx`, `Select.tsx`).
브라우저 기본 컨트롤은 톤이 안 맞고, **네이티브 `<select>` 의 펼침 목록은 OS 가 그려서
CSS 가 닿지 않는다.** 재생기의 탐색(드래그)은 브라우저가 실제 길이를 아는 경우에만 허용한다 —
자세한 이유는 6번 참고.

**한글에 모노스페이스·uppercase·넓은 자간을 쓰지 말 것.** uppercase 는 한글에 효과가 없고,
모노스페이스 폰트에 한글 글리프가 없어 다른 폰트로 대체되면서 의도보다 얇아진다.
모노스페이스는 **숫자만 들어가는 자리**(카운터, 재생 시간, 요약 개수, 탭 개수)에만 쓴다.

### 구현된 화면 — 명세 28개 엔드포인트 전부 커버

| 화면 | 경로 | 내용 |
|---|---|---|
| 로그인/회원가입 | `/login` | 한 화면에서 모드 전환. 가입 시 자동 로그인. 소셜 로그인 버튼(공급자 로고 포함, 서버에 설정된 것만) |
| 소셜 로그인 콜백 | `/oauth2/callback/:provider` | state 검증 후 인가 코드를 토큰으로 교환 |
| 홈 | `/` | **파일철 탭**(전체·보낸·받은·팀) + 페이지네이션 + 처리 중이면 4초 갱신.<br>**목록 아래에 남기기 블록이 붙어 있어** 페이지 이동 없이 인계를 남긴다 |
| 새 인계 남기기 | `/cards/new` | 홈과 같은 `NewCardForm` 을 쓰는 단독 화면. 헤더 버튼은 홈으로 스크롤하므로 평소엔 오지 않는다 |
| 카드 상세 | `/cards/:id` | status 2초 폴링, 단계 이름이 붙은 진행 바, 요약 블록(핵심 내용·할 일·이슈), 전체 내용, 재처리/삭제 |
| 팀 | `/team` | 팀 없을 때: 팀 만들기 · 가입 신청(표) · 내 신청 내역(표)<br>팀 있을 때: 팀원 목록(팀장이 맨 위) · (팀장) 신청 승인/거절 · 팀장 위임 · 내보내기 · 나가기/삭제 |
| 계정 설정 | `/settings` | 표시 이름 변경, 비밀번호 변경, 계정 삭제 |

### 파일 구조

```text
src/
  api/          types.ts, client.ts, endpoints.ts, tokens.ts
  auth/         AuthContext.tsx, AuthProvider.tsx
  components/   AppLayout.tsx, StatusBadge.tsx, ProviderMark.tsx, Select.tsx,
                NewCardForm.tsx, Recorder.tsx, AudioFilePicker.tsx,
                AudioPreview.tsx, AudioPlayer.tsx
  i18n/         messages.ts, LanguageContext.tsx, LanguageProvider.tsx, LanguageToggle.tsx
  hooks/        useAudioRecorder.ts, useHandoverCard.ts
  lib/          format.ts, languages.ts, oauth.ts, summary.ts
  pages/        LoginPage.tsx, OAuthCallbackPage.tsx, CardListPage.tsx,
                NewCardPage.tsx, CardDetailPage.tsx, TeamPage.tsx, SettingsPage.tsx
  App.tsx, main.tsx, styles.css, vite-env.d.ts
vite.config.ts, vercel.json, .env.example, index.html, public/favicon.svg
PROJECT_STATUS.md, BACKEND_REQUEST_EMAIL_VERIFY.md
```

### 주요 구현 포인트

- **토큰**: localStorage 저장. 모든 요청에 Bearer 자동 부착. **401 이면 리프레시로 재발급 후 원래
  요청을 자동 재시도**하고, 동시에 여러 요청이 401 을 맞아도 리프레시는 한 번만 나감(single-flight).
  리프레시까지 실패하면 토큰 폐기 후 로그인 화면으로.
- **비동기 처리 대응**: 카드 생성은 즉시 `{id, status}` 만 반환되므로 상세 화면에서 2초 간격 폴링.
  `COMPLETED`/`FAILED` 도달 시 폴링 중단. 화면을 벗어나도 서버 처리는 계속됨.
- **녹음**: MediaRecorder. `recorder.start()` 를 timeslice 없이 호출 — 조각으로 받아 이어 붙이면
  재생 불가능한 파일이 나온 사례가 있었고, 업로드는 종료 후 한 번뿐이라 조각낼 이유가 없음.
  실시간 음량 파형, 0바이트 녹음 감지, 마이크 권한 거부 안내 포함.
- **수신자 지정**: 팀에 속해 있으면 `GET /api/teams/me` 로 받은 **팀원 목록에서 드롭다운으로 고른다.**
  이름으로 찾는 `/api/members/lookup` 은 명세상 **완전 일치**만 지원해서(부분 일치는 이메일이
  노출되므로 의도된 제약) 한 글자만 달라도 못 찾는다. 팀원 목록에는 이메일이 이미 들어 있으므로
  타이핑해서 맞출 이유가 없다. 팀 밖 사람에게 보낼 때만 "직접 입력…" 으로 이름·이메일을 넣는다.
- **권한 오류 문구**: 소유자가 아닌 카드를 삭제·재처리하면 서버가 존재를 숨기려고 403 대신
  **404** 를 준다. "찾을 수 없음" 을 그대로 보여주면 오해하므로 "카드를 만든 사람만 삭제할 수
  있습니다" 로 번역해서 표시한다.
  (근본 해결은 `HandoverCardResponse` 에 `owner: boolean` 추가 — 9번의 백엔드 요청 목록 참고.)
- **카드 3분류(보낸/받은/팀)**: 목록에는 내 카드만 오지 않는다. 카드에 만들 당시 작성자의
  팀이 박히고 그 팀원은 남의 인계도 함께 볼 수 있어서, **다른 팀원끼리 주고받은 카드가
  섞여 온다.** 응답에 소유자를 알려주는 값이 없어 `senderName`/`receiverName` 을 내 이름과
  맞춰 보는 방식으로 나눈다. 동명이인이면 어긋난다 — `owner` 플래그가 생기면 그 값으로 바꿀 것.
- **보내는 사람을 계정 이름으로 고정**: 위 분류가 이 이름에 의존하므로 고칠 수 있게 두면
  내가 만든 카드가 "받은 카드" 쪽으로 넘어간다. 표시될 이름은 설정에서 계정 이름을 바꾼다.
  **이미 만든 카드에는 소급되지 않는다** (카드마다 만들 때 값이 박히는 구조).
- **요약이 늦게 저장되는 경우 대비**: `COMPLETED` 인데 요약이 비면 2초 간격으로 3번 더
  확인한다. 서버가 요약을 저장하기 전에 상태를 먼저 바꾸면 그 순간을 본 화면이 폴링을 멈추고
  빈 요약으로 굳어 새로고침해야만 보이는 상황이 된다.

## 4. 백엔드 API 정보

- **Base URL**: `https://api.handover-card.o-r.kr`
- **Swagger**: `https://api.handover-card.o-r.kr/swagger-ui/index.html`
- **OpenAPI JSON**: `https://api.handover-card.o-r.kr/v3/api-docs`
  (저장소에 사본을 두지 않는다. 서버가 살아 있으면 언제든 받을 수 있고, 사본은 금방 낡는다.)
- 인증: JWT (Bearer), access/refresh 구조. `TokenResponse { accessToken, refreshToken, tokenType, expiresInSeconds }`

**카드 상태 enum**: `RECEIVED → TRANSCRIBING → TRANSCRIBED → SUMMARIZING → COMPLETED` (또는 `FAILED`)

**요약 구조**: `summary { keyPoints[], actionItems[], blockers[] }`, 각 항목이 `{ source, target }`
→ 원문/번역 양쪽을 다 주므로 언어 토글 UI 가능

## 5. 이전에 "확인 필요" 였던 항목 — 대부분 해결

1. ✅ **오디오 업로드 방식**: `multipart/form-data`, **파트 이름은 `audio`**.
   (OpenAPI 명세에 requestBody 가 누락돼 있어 실서버 시도로 확인. `file` 은 400 반환.)
   쿼리파라미터로 `senderName`, `receiverName`, `receiverEmail`(선택), `sourceLanguage`, `targetLanguage` 전달.
2. ✅ **언어 코드**: ISO 639-1 두 글자 (`ko`, `en`).
3. ⚠️ **CORS**: `http://localhost:5173` 은 **이미 허용됨** (`authorization` 헤더 포함).
   그러나 **등록되지 않은 origin 은 preflight 403.** → 배포 도메인은 백엔드에 등록 요청 필요.
   대비책으로 `vercel.json` 에 `/api/*` → 백엔드 rewrite 를 넣어둠. 서버측에서 프록시하므로
   **백엔드가 도메인을 안 열어줘도 배포가 동작함.** 개발 중에도 `vite.config.ts` 프록시를 사용.
4. ⚠️ **OAuth**: 구현 완료. 단 **공급자 콘솔에 리다이렉트 URI 등록이 필요** — 아래 10번 참고.
5. ⬜ `api.handover-card.o-r.kr` 루트의 로그인 화면 정체는 여전히 미확인 (영향 없음).

**추가 확인 사항**: API 는 에러 응답 본문 형식을 전혀 명세하지 않음(모든 엔드포인트가 200 만 정의).
클라이언트는 `message`/`error`/`detail`/`title` 키를 순서대로 훑고 실패하면 상태코드별 기본 문구로 대체.

## 6. 해결된 이슈 기록

- **미리듣기 재생 실패 (해결)**: 녹음 결과를 Chrome 의 `<audio>` 가
  `DEMUXER_ERROR_COULD_NOT_OPEN` 으로 열지 못하던 문제. 원인은 `recorder.start(250)` 처럼
  **timeslice 를 줘서 250ms 조각으로 받아 나중에 이어 붙인 것**. 그렇게 만든 webm 은
  컨테이너 헤더에 전체 길이가 없어 재생도 탐색도 안 됐다.
  `recorder.start()` 로 바꿔 브라우저가 완결된 파일 하나를 만들게 하자 해결됐고,
  길이 정보가 들어가면서 **재생·드래그 탐색까지 정상 동작한다.**
  업로드는 어차피 녹음이 끝난 뒤 한 번뿐이라 조각낼 이유가 없었다.

- **길이를 Infinity 로 읽을 때의 우회는 쓰지 말 것**: 재생 위치를 큰 값으로 옮겨
  브라우저가 길이를 계산하게 만드는 방법이 널리 쓰이지만, 오디오가 끝에 멈춰
  **재생이 아예 막힌다.** `decodeAudioData` 로 길이를 재는 방법도 멀쩡한 파일을
  거부할 때가 있어 신뢰할 수 없다. 재생 가능 여부는 오디오 엘리먼트의 `error` 이벤트만 믿는다.

## 7. 알려진 이슈

- **가입 시 이메일 소유 확인이 없다 (백엔드 사안).** 카드 열람 권한이 `receiverEmail` 일치로
  결정되는데 아무 이메일로나 가입이 되므로, 남의 이메일로 가입해 그 사람 앞으로 온 카드를
  열어볼 수 있다. 프론트만으로는 막을 수 없다 — 명세에 인증 메일 관련 엔드포인트가 없다.
  → 백엔드 요청서를 `BACKEND_REQUEST_EMAIL_VERIFY.md` 에 정리해 뒀다.
  최소 수정(회원에 `emailVerified` 플래그 + 카드 조회 조건 한 줄)만으로 구멍은 닫힌다.
- 미리듣기 문제는 6번과 같이 해결됨.

## 8. 남은 일 (2026-08-14 기준)

**git 은 정리됐다.** 저장소가 프로젝트 폴더에 있고(`main` 브랜치), 깃헙 private 저장소
`llll1211llll1211/handover-card` 에 올라가 있다.
디자인·다국어 작업은 `design` 브랜치에서 하고 `main` 에 합쳤다.

> 참고: 예전에 홈 디렉터리(`C:\Users\a0109`)에 커밋 0개짜리 `.git` 이 있었다. 그대로 두면
> 홈에서 `git add -A` 를 했을 때 `.ssh`·`.gnupg` 가 통째로 스테이징된다. 아직 남아 있으면 정리할 것.

1. **Vercel 배포** 후 실제 URL 에서 전체 플로우 재확인.
   **마이크는 https 가 필수**라 배포 환경에서 녹음이 되는지 반드시 확인해야 한다 (Vercel 은 기본 https).
2. 배포 URL 확정 후 **OAuth 리다이렉트 URI 등록 요청** (10번 참고. 부탁할 것 전체 목록은 9번).
   **프리뷰 주소가 아니라 고정된 프로덕션 도메인을 등록해야 한다** — 프리뷰는 push 마다 바뀐다.
   커스텀 도메인을 붙일 거면 그것까지 정하고 요청할 것. 안 그러면 두 번 요청하게 된다.
   로컬 주소(`http://localhost:5173/...`)는 굳이 등록 요청하지 않는다. 데모는 배포본으로 찍는다.
3. 데모 시나리오 리허설: 인계자 계정으로 녹음 → 인수자 계정으로 로그인해 카드 확인.
   **주의**: 수신자가 카드를 보려면 `receiverEmail` 이 가입된 이메일과 일치해야 하거나
   같은 팀이어야 함 (카드에 생성 당시 작성자 팀이 박히고 그 팀원이 함께 볼 수 있음).
   앱에서 팀 생성·승인이 가능하므로 계정 2개로 리허설 가능.
4. **데모용 데이터 정리** — 현재 테스트 계정 이름이 `ㅇ`, 팀 이름이 `dddd` 다.
   카드 머리글이 화면에서 가장 크게 보이는 부분이라 실제 이름으로 바꿔야 한다.
   이름은 설정 화면에서 변경 가능. 팀은 이름 변경 API 가 없어 새로 만들어야 함(예: `결제팀`).
5. **촬영용 음성 파일 준비** — 라이브 녹음보다 미리 준비한 파일을 올리는 쪽이 안전하다.
   말 더듬거나 소음이 들어가면 다시 찍어야 하고, STT 결과가 안정적이면 요약 품질도 예측 가능해진다.
   **영어 인계 음성을 `en → ko` 로 돌리면** "외국인 개발자와 협업" 서사가 화면에 바로 보인다.
   포맷은 mp3 나 wav 권장 (미리듣기·탐색이 정상 동작하고 STT 호환성이 가장 넓다.
   webm·mp4 도 서버가 처리하는 것은 확인됨).
6. 8/17 전 데모 영상 촬영

### 검토 중이던 UI 항목 (선택)

- 파일명을 16px 까지 키우거나 두 줄로 내리는 것 — 지금은 14px 말줄임
- 헤더의 「새 인계 남기기」 버튼은 이제 홈으로 스크롤만 한다. `/cards/new` 경로는 살아 있지만
  들어갈 일이 없으므로, 지울지 남길지 정할 것
- 앰버가 상시로 보이는 자리가 없다(로고 표식을 뺀 뒤로 녹음 중에만 나타난다).
  현재 탭 밑줄이나 「처리 중」 카드의 왼쪽 띠에 쓰면 정보도 함께 전달된다

## 9. 백엔드에 부탁할 것 (한곳에 모음)

프론트에서 할 수 있는 일이 아닌 것들이다. 마감이 8/17 이므로 위에서부터 부탁한다.

| 순위 | 부탁 | 없으면 | 근거 |
|---|---|---|---|
| **1** | 회원에 `emailVerified` 플래그 + 카드 조회 조건에 `AND emailVerified = true` | 남의 이메일로 가입해 그 사람 앞으로 온 카드를 열람할 수 있다 | `BACKEND_REQUEST_EMAIL_VERIFY.md` |
| **2** | 배포 도메인의 **OAuth 리다이렉트 URI 등록** (콘솔 작업, 1분) | 소셜 로그인 버튼이 `redirect_uri_mismatch` 로 죽는다 | 10번 |
| 3 | `HandoverCardResponse` 에 `owner: boolean` 추가 | 보낸/받은 카드 구분을 이름 대조로 때우고 있고, 삭제·재처리 권한 문구도 404 를 번역해 쓰고 있다 | 3번 주요 구현 포인트 |
| 4 | 가입 신청 취소 API (`DELETE /api/teams/join-requests/{requestId}`, 본인 PENDING 만) | 잘못 신청하면 팀장이 거절할 때까지 못 푼다. 대기 중이면 다른 팀 신청도 막힌다 | 명세에 오퍼레이션 자체가 없음 |
| 5 | 인증 메일 발송·검증 엔드포인트 | 구멍은 1번으로 막히지만 아무도 인증을 못 받는다 | `BACKEND_REQUEST_EMAIL_VERIFY.md` 3단계 |
| 6 | **비밀번호 재설정(찾기)** — 발송·확인 엔드포인트 2개 | 비밀번호를 잊으면 계정을 되찾을 방법이 아예 없다 | `BACKEND_REQUEST_EMAIL_VERIFY.md` 7단계 |
| 7 | 에러 응답 본문 형식 `{ message }` 통일 | 프론트가 `message`/`error`/`detail`/`title` 을 순서대로 추측한다 | 5번 추가 확인 사항 |
| 8 | 요약이 비었을 때의 구분 (실패면 `errorMessage`, 정말 없으면 빈 배열) | 요약이 깨져도 `COMPLETED` 로 와서 화면이 원인을 설명할 수 없다 | 7번 알려진 이슈 |
| 9 | **인식 결과 교정** — `PATCH /api/handover-cards/{id}/transcript` 로 원문을 고치면 번역·요약을 그 텍스트로 다시 돌린다 (소유자만) | STT 가 고유명사를 틀리면 번역·요약까지 함께 틀린다. 인수자는 외국인이라 원문 대조로 알아챌 수 없다 | 아래 설명 |

**1·2번은 데모 전에 반드시 필요하다.** 3~9번은 없어도 데모는 돌아가고,
발표에서 "다음 단계" 로 정리하면 된다.

**5번과 6번은 메일 발송·토큰 관리를 그대로 공유한다.** 한쪽을 만들어 두면 다른 쪽은
엔드포인트 두 개와 메일 본문만 더 얹으면 되므로 **묶어서 부탁하는 편이 싸다.**
둘 중 하나만 된다면 5번이 먼저다 — 그쪽이 실제로 뚫려 있는 구멍을 막는다.

### 9번(인식 결과 교정)에 대해 — 마감 뒤로 미룬 이유

제품으로는 3~8번 중 가장 값어치가 크지만 **작업량도 가장 크다.** 백엔드가 파이프라인을
「STT 부터」와 「번역부터」로 쪼개야 한다. 지금 있는 `reprocess` 는 원본 오디오에서 다시
시작하므로 같은 오류가 또 나와 교정 수단이 못 된다. 카드 생성도 음성 파일로만 되고
텍스트로 만드는 경로가 없어, **프론트만으로는 어떤 방법으로도 만들 수 없다.**

브라우저에만 저장하는 편집으로 흉내내지 말 것. 카드는 인수자와 공유되는 서버 객체라,
**고친 사람은 됐다고 믿고 인수자는 여전히 틀린 글을 본다.** 없는 것보다 나쁘다.

부탁하기 전에 정해야 할 것:

- 고칠 수 있는 사람은 **소유자(말한 사람)만**. 인수자가 고치면 인계 내용의 출처가 흐려진다.
- **원문만 고치게 한다.** 번역문까지 손대게 하면 요약을 다시 돌릴 때 그 편집이 덮여 버린다.
- 고치는 동안의 status — 번역·요약이 다시 도는 사이 `SUMMARIZING` 으로 되돌리면
  화면은 이미 그 상태를 폴링할 줄 아므로 프론트 추가 작업이 거의 없다.
- 인수자가 이미 읽은 뒤에 고쳤을 때 알릴 방법은 이번 범위 밖으로 둔다.

## 10. 소셜 로그인을 실제로 쓰려면 (프론트 코드는 완료, 외부 등록 필요)

프론트는 `GET /api/auth/oauth2/providers` 로 받은 `authorizationUri`/`clientId`/`scopes` 로
인가 URL 을 직접 조립하고, state 를 sessionStorage 에 저장해 콜백에서 검증한다.
**리다이렉트 URI 형식**은 다음과 같다.

```text
{프론트주소}/oauth2/callback/google
{프론트주소}/oauth2/callback/github
```

즉 로컬 개발은 `http://localhost:5173/oauth2/callback/google`,
배포 후에는 `https{배포도메인}/oauth2/callback/google` 이 된다.

**해야 할 일**: 이 URI 들을 Google Cloud Console 의 OAuth 클라이언트 "승인된 리디렉션 URI" 와
GitHub OAuth App 의 "Authorization callback URL" 에 등록해야 한다.
클라이언트 ID 를 백엔드가 발급받아 설정했으므로 **콘솔 접근 권한이 백엔드 담당자에게 있을 가능성이 높다**
→ 배포 URL 확정 후 위 두 주소 등록을 요청할 것.

등록 전에는 소셜 로그인 버튼을 눌러도 공급자가 `redirect_uri_mismatch` 를 반환한다.
**이메일 로그인은 영향받지 않으므로 데모에는 지장이 없다.**

## 11. 로컬 실행

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # 타입체크 + 프로덕션 빌드
```

`.env` 는 로컬 개발에서 불필요 (비워두면 `/api` 요청이 Vite 프록시를 타서 CORS 무관).
배포 시 Vercel rewrite 를 쓰면 `VITE_API_BASE_URL` 도 비워둔 채로 동작함.

## 12. 참고 링크

디자인 방향을 정할 때 만든 시안 페이지 (비공개, 본인만 열람 가능):

- 전체 톤 3안 비교 — <https://claude.ai/code/artifact/59062d33-4e36-471c-81c5-db4201d6b279>
  (안 A 「교대 노트」 채택, 안 B 「해질녘」 다크, 안 C 「말풍선」)
- 녹음 블록 3안 비교 — <https://claude.ai/code/artifact/e2258b5a-3d58-4ddf-b071-1423eb7f658e>
  (안 3 「테이프 카운터」 채택)

색을 조정하거나 다른 안으로 바꾸고 싶을 때 다시 열어보면 된다.
